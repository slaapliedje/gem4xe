/* strings.h -- BSD's string header.  Everything an ST source takes from
 * it that this machine has is either ISO (and in <string.h>) or non-ISO
 * (and in <support.h>). */
#ifndef GEM4XE_STRINGS_H
#define GEM4XE_STRINGS_H
#include <string.h>
#include "support.h"
#endif
